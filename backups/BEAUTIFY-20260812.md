# 第二版官网美化记录

## 已建立的回退基线

改造前源码快照位于 `site-before-beautify-20260812-225802/`，归档文件为 `site-before-beautify-20260812-225802.tar.gz`，并已生成 SHA-256 校验文件。快照不包含可重新生成的 `node_modules` 与 `dist` 目录。

如需恢复改造前源码，请在 `build-v2` 目录执行：

```zsh
./backups/restore-site-before-beautify.sh
```

脚本会要求手动输入 `RESTORE`，避免误覆盖；恢复后在 `site` 目录运行 `npm run build` 即可验证。

## 本次改造范围

本次未改动任何文章、案例内容、路由或数据文件。首页升级为“证据账本”式双入口：使用深蓝档案底、低对比栅格、暖金路径线、栏目编号与明确的进入动作，突出「明鉴」与「研究与判断」两条内容路径。栏目页新增带刻度标记的黏性导航，并为入口卡片、案例卡片和列表补充克制的悬停反馈。

## 验证结果

生产构建已于 2026-08-12 完成，共生成 12 个静态页面。首页及「明鉴」栏目页均已通过本地浏览器检查，确认标题、双入口、页眉导航、正文和页脚均能正常显示。`npm run check` 未执行，因为项目当前没有安装可选的 `@astrojs/check` 与 TypeScript；为保持依赖基线，本次未安装任何新依赖。
